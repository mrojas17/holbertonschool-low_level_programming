#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int Numb;

	for (Numb = 0; Numb < 100; Numb++)
	{
		putchar(n / 10 + '0');
		putchar(n % 10 + '0');
	}

	putchar('\n');
	return (0);
}
